---
- project:
    name: matofederorg/zuul-config
    default-branch: main
    merge-mode: "squash-merge"
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop
    post:
      jobs:
        - noop
    tag:
      jobs:
        - noop

- project:
    name: matofederorg/k8s-cluster-api-provider
    default-branch: main
    merge-mode: "squash-merge"
    periodic-daily:
      jobs:
        - noop
        branches:
        - main
        - test_br
